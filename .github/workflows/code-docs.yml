name: Generate code documentation

on:
  push:
    branches:
      - 'HIA-1079_code_doc_poc'

jobs:
  generate-docs:
    if: github.actor != 'dependabot[bot]'
    name: 🧹 Generate code docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5
      - name: Generate code docs
        run: ./gradlew codeDoc
