# Automated management of Dependabot PRs
name: Dependabot
on: workflow_dispatch # Temp removal of pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}

      - uses: ministryofjustice/hmpps-probation-integration-services/.github/actions/dependabot-automerge@b9b6fdfdb69815a6dc432538e0cb0f3ebc27cecd # main
        with:
          token: ${{ steps.app-token.outputs.token }}
